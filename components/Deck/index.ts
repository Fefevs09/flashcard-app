import { DeckIcon } from './DeckIcon';
import { DeckRoot } from './DeckRoot';
import { DeckInfo } from './DeckInfo';

const Deck = {
  Root: DeckRoot,
  Title: DeckInfo,
  Icon: DeckIcon
};

export default Deck;
